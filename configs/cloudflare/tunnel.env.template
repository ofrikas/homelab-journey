# Cloudflare Tunnel Configuration
# Get your token from: Cloudflare Dashboard → Zero Trust → Networks → Tunnels → Create

# Your tunnel token (keep this secret!)
CLOUDFLARE_TUNNEL_TOKEN=REPLACE_WITH_YOUR_TUNNEL_TOKEN
